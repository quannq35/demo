# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'googlemap_test' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for googlemap_test
  pod 'GoogleMaps'

  target 'googlemap_testTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'googlemap_testUITests' do
    # Pods for testing
  end

end
